{
  "name": "aws-serverless-ticket-system-backend-tutorial",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/handlers/index.ts --bundle --platform=node --target=node22 --outfile=dist/handlers/index.js --sourcemap",
    "zip": "zip -r deployment-package.zip dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.946.0",
    "@aws-sdk/lib-dynamodb": "^3.946.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.159",
    "@types/node": "^25.3.3",
    "esbuild": "^0.27.3",
    "typescript": "^5.9.3"
  }
}
